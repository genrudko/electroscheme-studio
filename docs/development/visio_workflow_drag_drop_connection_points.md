# Patch 035 repair1 — Visio workflow canvas

Repair for patch 035 after `CanvasViewport.vue` did not contain `onPaletteDrop`.

Implemented:

```text
onPaletteDrop
application/x-electroscheme-command drag/drop
createCircuitBreaker
symbolObjects
symbolAttachmentLines
attachSymbolToNearestSlot
findNearestBusbarSlot
selectionOverlayRect
selectionHandles
MiniToolbar
white ISO page on gray workspace
busbar slots as connection points
ГОСТ voltage palette preserved
```
